/**
 * Auth-routes (inloggning, registrering) — byggs i Fas 1 med Better Auth.
 */
export default function AuthPlaceholderPage() {
  return (
    <main className="mx-auto max-w-md px-6 py-16">
      <h1 className="text-xl font-semibold">Inloggning</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Better Auth med organisationer kommer i Fas 1.
      </p>
    </main>
  );
}
